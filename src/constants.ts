export enum ChannelEvent {
    CONNECT = 'connect',
    DISCONNECT = 'disconnect',
    SET = 'setChannel',
    JOIN = 'join',
    LEAVE = 'leave'
} 
export enum PlaybackEvent {
    CONNECT = 'connect',
    DISCONNECT = 'disconnect',
    JOIN = 'join',
    LEAVE = 'leave'
}